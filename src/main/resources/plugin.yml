name: "${name}"
main: "${group}.Pl3xMapClaims"
version: "${version}"
api-version: "${apiVersion}"
load: "POSTWORLD"
authors: ${authors}
description: "${description}"
website: "${website}"

softdepend:
  - Pl3xMap
  - ClaimChunk
  - GriefDefender
  - GriefPrevention
  - PlotSquared
  - WorldGuard